---
// Click button, get confetti!
// Styled by Tailwind :)
---

<button
  class="appearance-none py-2 px-4 bg-blue-400 text-white font-semibold rounded-full shadow-md hover:bg-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-200 focus:ring-opacity-75"
>
  <slot />
</button>

<style>
  button {
    transition: all 0.15s ease;
  }

  button:hover {
    transform: translateY(-5px);
  }

  button:active {
    transform: translateY(0);
    transform: scale(0.95);
  }
</style>
